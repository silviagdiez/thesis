function v = computeClass()
%============================================
% Computes Class vector
%============================================

mydir = '../Self-avoiding/Cycles';
dirs = {'../AdjacencyIntervalsSelected','../AdjacencyIntervalsRandom'};

mylist = ls(mydir);
numItems = (length(mylist)/37);
n = 0;
cbegin = 1;
cend = 0;
	
for i=1:numItems 
	'%%%%%%%%%%%%%% NEW ITEM %%%%%%%%%%%%%%'
	cend = cbegin + 35;
	myfile = mylist(cbegin:cend)
	cbegin = cend + 2;
	
	for j=1:length(dirs)
        list2 = ls(dirs{j});
        if l
    end
		
end
